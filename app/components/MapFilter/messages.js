/*
 * MapFilter Messages
 *
 * This contains all the text for the MapFilter component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.MapFilter';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MapFilter component!',
  },
});
