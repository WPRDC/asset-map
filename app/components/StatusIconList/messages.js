/*
 * StatusIconList Messages
 *
 * This contains all the text for the StatusIconList component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.StatusIconList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the StatusIconList component!',
  },
});
