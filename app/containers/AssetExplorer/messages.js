/*
 * AssetExplorer Messages
 *
 * This contains all the text for the AssetExplorer container.
 */
import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AssetExplorer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AssetExplorer container!',
  },
});
